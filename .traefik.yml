displayName: Full URL Rewrite
type: middleware
import: github.com/abramad-labs/url-rewrite-traefik

summary: "Rewrite full request URL using regular expressions"

testData:
  regex: ^//(\w+)\.example\.com/(.+)
  replacement: //$1.example.com/$1/$2
